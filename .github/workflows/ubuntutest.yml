name: ubuntutest
on: [workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Build
      env:
        a: ${{ secrets.KEYSTORE }}
        b: ${{ secrets.SECRETEST }}
      run: |
        echo ${b} | base64 -di
